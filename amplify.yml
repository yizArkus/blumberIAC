# Generated by make setup (monorepo: appRoot=front-end). Edit if you change the path.
version: 1
applications:
  - appRoot: front-end
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
